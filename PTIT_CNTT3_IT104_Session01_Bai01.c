//
#include <stdio.h>

int main()
{
    for (int i = 0; i < n; i++)
    {
        printf("Hello\n");
    }
    return 0;
}
// Độ phức tạp của thuật toán: O(n) tại vì nó được lặp lại O(1) + O(1) + ... + O(1) = O(n)